
size = int(input("Enter number of candidates: "))

candidates = []
for i in range(size):
    candidate = input(f"Enter candidate ({i+1} of {size}): ")
    candidates.append(candidate)

# x = candidates
# print(f"{x}")
